# components/layout

Page chrome for the admin dashboard.

| Component | Purpose |
| --- | --- |
| [PageHeader.tsx](PageHeader.tsx) | Page title and header actions. |

The dashboard's navigation is built into the pages themselves — `DashboardView` owns its
own sidebar — so this folder is deliberately thin compared with
[Frontend/src/components/layout/](../../../../../Frontend/src/components/layout).
