# src/hooks

**Currently empty.** Reserved for React hooks shared across features.

Feature-scoped hooks belong with their feature. Move one here only when a second feature
genuinely needs it — a hook shared by exactly one caller is just indirection.

Git does not track empty directories, so this folder does not survive a clone without this
file.
