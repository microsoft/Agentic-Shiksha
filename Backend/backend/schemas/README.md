# backend/schemas

**Currently empty.** Intended for Pydantic request and response models, split out of
[main.py](../main.py).

Git does not track empty directories, so this folder does not survive a clone — this
README is what makes it exist. If the split is not going to happen, delete the folder
rather than leaving a placeholder that suggests structure the code does not have.

Request and response models presently live inline in `main.py`.
