The previous TikZ code had issues. Here is the feedback:

{feedback}

Here is the previous TikZ code you wrote (with line numbers for reference):
```tikz
{previous_code_numbered}
```

A rendered image of the current output is attached so you can see exactly what the
diagram looks like. Use BOTH the feedback text AND the image to understand what
needs to be fixed.

Produce ONLY targeted edits using <<<SEARCH>>> / <<<REPLACE>>> blocks.
Each block finds an exact snippet in the old code and replaces it.

Format (you may output multiple blocks):
<<<SEARCH>>>
exact lines from the old code to find
<<<REPLACE>>>
replacement lines (may be empty to delete, or new lines to add/change)
<<<END>>>

Rules:
- The SEARCH text must appear EXACTLY once in the old code (copy-paste it verbatim).
- To INSERT new lines, pick a nearby existing line as SEARCH and include it
  unchanged together with the new lines in REPLACE.
- To DELETE lines, put them in SEARCH and leave REPLACE empty.
- Output ONLY <<<SEARCH>>>/<<<REPLACE>>>/<<<END>>> blocks — no other text.
- If the changes are too extensive (>60 % of lines), output the COMPLETE rewritten
  TikZ code body inside ```tikz ... ``` fences instead of edit blocks.
